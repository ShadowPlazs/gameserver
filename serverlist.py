import servers
from servers import backup

def on_server(detected):
  if new.server(detected):
    reload.servers("reload")
    action.showserver(all)
    
def if.on_detected(api_err):
  if server.error(detected):
    print('An unexpected error has occured in the module "Api". Please restart the application to continue your gameplay on a server.')
    reboot.server(if):
      if server.unstable():
        reboot.server(as):
          cancel
          
 def on_error():
  if system.error(detected):
    re.system(re.boot)
  else:
    action.cancel(action)
  elif
    action.crash(action.i)
